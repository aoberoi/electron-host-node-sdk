{
  "name": "electron-host-node-sdk",
  "version": "1.0.0",
  "description": "A test app to use @slack/client inside an electron app",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^3.0.4"
  }
}
