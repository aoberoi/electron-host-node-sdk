{
  "name": "electron-host-node-sdk",
  "version": "1.0.0",
  "description": "A test app to use @slack/client inside an electron app",
  "main": "main.js",
  "scripts": {
    "start": "ELECTRON_RUN_AS_NODE=1 electron ."
  },
  "devDependencies": {
    "electron": "2.0.9"
  },
  "dependencies": {
    "@slack/client": "^4.8.0"
  }
}
